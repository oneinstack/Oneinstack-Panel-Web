import type TimeBean from "./TimeBean";
export default class TimeRender {
    /**
     * 渲染函数
     */
    static render: any;
    static uuid: number;
    /**
     * 获取唯一id
     * @returns
     */
    static getId: () => string;
    /**
     * 开始渲染
     */
    static run: () => void;
    /**
     * 停止渲染
     */
    static stop: () => void;
    /**
     * 最后一次更新时间
     */
    private static updateLastTime;
    /**
     * 获取帧渲染时间
     * @returns
     */
    private static getUpdateLastTime;
    /**
     * 更新渲染
     */
    static update: () => void;
    static map: {
        [key: string]: TimeBean;
    };
    /**
     * 添加一个渲染任务
     * @param obj
     * @returns
     */
    static add: (obj: TimeBean) => string;
    /**
     * 移除一个渲染任务
     * @param obj
     * @returns
     */
    static remove: (uuid: string) => void;
}
