import TimeBean, { TimeBeanParam } from './TimeBean';
export default class Time {
    /**
     * 创建一个动画任务
     */
    static to: (options: TimeBeanParam | TimeBeanParam[]) => {
        /**
         * 动画对象
         */
        bean: any;
        kill: () => void;
    } | undefined;
    /**
     * 创建一个链式动画任务 ,loop强制为0
     */
    static toLine: (options: TimeBeanParam, paused?: boolean) => TimeToLineBean;
    /**
     * 通过唯一name停止动画
     * @param name
     */
    static stop: (...name: string[]) => void;
}
export interface TimeToLineBean {
    bean: TimeBean;
    nextTo: {
        ctx: TimeToLineBean;
    };
    preTo: {
        ctx: TimeToLineBean;
    };
    toLine: (options: TimeBeanParam, needFinalKill?: boolean) => any;
    /**
     * 清理当前动画，后续的也会被清理
     * @returns
     */
    killSelf: () => void;
    /**
     * 清理所有动画
     * @returns
     */
    kill: () => void;
}
