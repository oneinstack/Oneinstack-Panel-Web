export default class TimeAction {
    /**
     * 匀速运动
     * @returns
     */
    static lerp({ start, end, percent }: any): any;
    /**
     * 16进制颜色转换
     * @returns
     */
    static lerpColor({ start, end, percent }: any): any;
}
